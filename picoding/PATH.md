# Pi Komutu - Kurulum

## Terminal'de `pi` komutunu her yerde çalıştırma

### Adım 1: `pi.cmd` konumunu PATH'e ekle

```cmd
setx PATH "%PATH%;D:\OpenCode\Llm-Runner-aio\picoding"
```

veya geçici olarak:

```cmd
set PATH=%PATH%;D:\OpenCode\Llm-Runner-aio\picoding
```

### Adım 2: Terminali yeniden başlat

### Adım 3: Herhangi bir klasörde çalıştır

```cmd
cd herhangi\bir\klasor
pi "kodları incele"
```
